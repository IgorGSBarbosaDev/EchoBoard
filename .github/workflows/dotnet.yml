name: .NET

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-test:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore workloads
        run: dotnet workload restore

      - name: Restore
        run: dotnet restore EchoBoard.sln

      - name: Build
        run: dotnet build EchoBoard.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test EchoBoard.sln --configuration Release --no-build
